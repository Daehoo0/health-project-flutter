import 'package:flutter/material.dart';

class ChatConsultationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Halaman Konsultasi', style: TextStyle(fontSize: 18)),
    );
  }
}
